export class Pagination {
    constructor(pageSize, pageNumber, pageTotal, data) {
        this.pageSize = pageSize;
        this.pageNumber = pageNumber;
        this.pageTotal = pageTotal;
        this.data = data;
    }
}
